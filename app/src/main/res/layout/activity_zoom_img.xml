<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context=".Olds.CropImage.ZoomImgActivity" >

    <ImageView
        android:id="@+id/btnOutZoom"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
         />

    <ImageView
        android:id="@+id/btnBackZoom"
        android:layout_width="@dimen/menu_size_item"
        android:layout_height="@dimen/menu_size_item"
        app:srcCompat="@mipmap/ic_close_white" />

</RelativeLayout>